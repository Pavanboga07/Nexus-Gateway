# Nexus relay (v0.3) — direct dependencies, pinned.
#
# Ported from nexus-v1: the relay is Postgres-backed (queue claims need
# SELECT ... FOR UPDATE SKIP LOCKED, so there is no SQLite backend).
# Every `==` matches a version installed in this repository's test
# environment. Nothing new is pinned beyond what nexus-v1 pins
# (cryptography); the other pins are carried over from the gateway's
# previous requirements at their installed versions.

fastapi==0.115.0
uvicorn[standard]==0.52.4
pydantic==2.9.0
httpx==0.28.1

# Database & Migrations (Postgres only)
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.0

# Cryptography (Ed25519 auth/envelope verification via app/identity)
cryptography==46.0.1

# Testing
pytest==9.1.1
